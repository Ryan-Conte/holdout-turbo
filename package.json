{
  "name": "holdout",
  "private": true,
  "packageManager": "npm@11.2.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "pngjs": "^7.0.0",
    "prisma": "^6.19.3",
    "turbo": "^2.5.0"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  }
}
